# TrialTracker analytics setup

Cloudflare Web Analytics beacon is installed in `templates/base.html`.

Cloudflare site: `trytrialtracker.com`

Google Search Console remains a separate ownership-verification step and is not configured by this code change.
